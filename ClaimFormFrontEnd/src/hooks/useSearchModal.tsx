import {ReactElement, useState} from "react";


const UseSearchModal = (SearchModel: ReactElement) => {

    const [visible, setVisible] = useState(true);

    return (
        <View>

        </View>
    );

    return
};

export default UseSearchModal;
